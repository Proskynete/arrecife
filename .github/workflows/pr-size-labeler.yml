name: "📏 Tamaño del PR"

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  size-label:
    name: Etiquetar por tamaño
    runs-on: ubuntu-latest
    steps:
      - name: 📊 Etiquetar
        uses: codelytv/pr-size-labeler@v1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          xs_label: "🤩 size/xs"
          xs_max_size: 10
          s_label: "🥳 size/s"
          s_max_size: 100
          m_label: "😎 size/m"
          m_max_size: 500
          l_label: "😖 size/l"
          l_max_size: 1000
          xl_label: "🤯 size/xl"
          fail_if_xl: "false"
          message_if_xl: >
            ⚠️ Este PR pasa de 1000 líneas. Comprueba que no esté resolviendo
            varias cosas a la vez y considera partirlo.
          files_to_ignore: "pnpm-lock.yaml *.lock docs/* CHANGELOG.md"
